# Environment Variables Template for Vercel

# Node Environment
NODE_ENV=production

# Optional: Rate limiting (future use)
# RATE_LIMIT_MAX_REQUESTS=100
# RATE_LIMIT_WINDOW_MS=60000

# Optional: Custom cache TTL in seconds (default: 600)
# CACHE_TTL=600
